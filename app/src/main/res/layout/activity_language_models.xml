<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_black"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:letterSpacing="0.08"
            android:text="LANGUAGE MODELS"
            android:textAllCaps="true"
            android:textColor="@color/text_white"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Optional neural language packs. Internet is used ONLY to download/update a pack once; after install, speech runs fully offline."
            android:textColor="@color/text_faint"
            android:textSize="12sp" />

        <!-- Storage summary -->
        <TextView
            android:id="@+id/tvStorageSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="@drawable/bg_surface_chip"
            android:padding="14dp"
            android:text="Installed model storage: 0 MB"
            android:textColor="@color/comm_green"
            android:textSize="13sp"
            android:textStyle="bold" />

        <!-- Language list (populated dynamically) -->
        <LinearLayout
            android:id="@+id/container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:orientation="vertical" />

        <TextView
            android:id="@+id/tvRelayNote"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:text="TIP: Relay nodes do NOT need any speech model. A relay only needs networking/routing/DTN — keep it light."
            android:textColor="@color/text_muted"
            android:textSize="12sp" />
    </LinearLayout>
</ScrollView>